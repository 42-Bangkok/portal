/**
 * Public resume page
 * if user allows public resume it is available at /r/[login]
 * else display 404
 * TODO: resume-ii
 * if current user is the same as [login], allow the user to edit additional information
 */
export default function Page() {
  return null;
}
